# `@rishijay/prettier-config`

> My Custom [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @rishijay/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@rishijay/prettier-config"
}
```
